from dataclasses import dataclass
from datetime import datetime
from typing import Optional

@dataclass
class Reminder:
    """
    📋 МОДЕЛЬ НАПОМИНАНИЯ - описывает структуру данных одного напоминания
    
    Атрибуты:
    - id: уникальный идентификатор в базе данных (автоинкремент)
    - user_id: ID пользователя Telegram (чтобы показывать только его напоминания)  
    - text: текст напоминания (что нужно сделать)
    - reminder_time: время срабатывания напоминания в формате 'ГГГГ-ММ-ДД ЧЧ:ММ'
    - created_at: когда было создано напоминание
    - is_completed: выполнено ли напоминание (True = отправлено, False = ожидает)
    """
    id: Optional[int]           # Может быть None до сохранения в БД
    user_id: int               # Целое число - ID пользователя Telegram
    text: str                  # Строка с текстом напоминания
    reminder_time: str         # Строка в формате '2024-01-25 15:30'
    created_at: str           # Строка в формате '2024-01-25 10:00:00'
    is_completed: bool        # Булево значение: True/False